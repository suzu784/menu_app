<%= form_with model: [post, post_comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '1', placeholder: "コメントをここに", class: "comment-textarea" %>
  <%= f.submit "送信", class: 'btn btn-primary' %>
<% end %>